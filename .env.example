# Backend Environment Variables (.env)
# 
# This file contains sensitive credentials needed for the NetSec AI Scanner backend.
# DO NOT commit this file to version control - it should never be shared publicly.
#
# Setup Instructions:
# 1. Copy this file and rename it to .env (remove .example)
# 2. Fill in the values below with your actual credentials
# 3. Keep .env in the .gitignore to prevent accidental commits
# 4. Never share your .env file with others or push it to repositories
#
# For local development:
#   - Get GOOGLE_API_KEY from: https://aistudio.google.com/app/apikey
#   - Create the backend/.env file and fill in the key

GOOGLE_API_KEY=your_gemini_api_key_here
